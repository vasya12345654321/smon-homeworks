# Домашнее задание: Prometheus. Часть 2

**Студент:** Misha Lukyanov

---

## Задание 1. Создание алерта

Создан файл `alert.rules.yml` с правилом:

```yaml
groups:
  - name: node_exporter_alerts
    rules:
      - alert: NodeDown
        expr: up{job="node_exporter"} == 0
        for: 1m
```

Node Exporter был остановлен для проверки алерта.

### Результат

Алерт перешёл в состояние **Pending**:

<img width="3840" height="1365" alt="задание 1" src="https://github.com/user-attachments/assets/8c0fd4b4-7001-480e-8dd5-55a0d15394f8" />


---

## Задание 2. Установка Alertmanager

Alertmanager установлен и подключён к Prometheus.

В конфигурацию `prometheus.yml` добавлен блок:

```yaml
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "localhost:9093"
```

### Результат

Алерт перешёл в состояние **Firing**:

<img width="3840" height="1402" alt="задание 2" src="https://github.com/user-attachments/assets/fff01599-b890-4996-b01c-9ef653dd51c4" />


Алерт отображается в Alertmanager:

<img width="3840" height="1552" alt="задание 2-2" src="https://github.com/user-attachments/assets/0c9ee620-fceb-435a-975e-fdf5f552eaaf" />


---

## Задание 3. Docker exporter

Для сбора метрик Docker был установлен cAdvisor:

```bash
docker run -d \
  --name=cadvisor \
  -p 8081:8080 \
  --volume=/:/rootfs:ro \
  --volume=/var/run:/var/run:ro \
  --volume=/sys:/sys:ro \
  --volume=/var/lib/docker/:/var/lib/docker:ro \
  gcr.io/cadvisor/cadvisor:latest
```

Добавлен в `prometheus.yml`:

```yaml
- job_name: 'cadvisor'
  static_configs:
    - targets: ['localhost:8081']
```

### Результат

Метрики доступны по endpoint:

<img width="3840" height="2035" alt="задание 3" src="https://github.com/user-attachments/assets/4e0d1bac-03f6-4df5-8acd-59f90f9d966d" />


Таргет в Prometheus:
<img width="3840" height="1665" alt="задание 3-1" src="https://github.com/user-attachments/assets/a3992771-8c9b-4353-a035-530dfc6c3466" />


---

## Задание 4. Grafana

Установлена Grafana и подключена к Prometheus.

Создан дашборд со следующими панелями:

* CPU (загрузка процессора)
* RAM (использование памяти)
* Docker (метрики контейнеров)

### Результат

<img width="3840" height="1765" alt="Снимок экрана 2026-04-18 120854" src="https://github.com/user-attachments/assets/004841ef-49fc-4783-b18f-cb75871ca567" />

---

## Итог

В ходе выполнения задания были:

* Настроены алерты в Prometheus
* Установлен и подключён Alertmanager
* Подключён Docker exporter (cAdvisor)
* Создан дашборд Grafana
